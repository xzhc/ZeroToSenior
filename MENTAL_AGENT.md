# 我的项目式工程学习协议

## 我的目标

我通过持续构建真实项目，扩大自己能够独立负责的范围：

```text

让一段代码运行

-&gt; 完成一个明确功能

-&gt; 负责一条完整用户路径

-&gt; 负责一个可部署、可维护的模块或服务

-&gt; 处理生产问题和系统取舍

-&gt; 帮助团队更可靠地交付

```

项目是主线，知识是解决项目问题的工具。我不以看完多少课程衡量成长，而以自己能否解释、实现、调试、验证和维护真实结果衡量成长。

## 默认状态：Build

任何时候，我只推进一个可以观察结果的工作项。

开始前写清四件事：

```text

当前目标：这一步要让谁获得什么结果？

完成条件：出现什么可观察行为才算完成？

当前事实：代码和系统现在实际是什么状态？

最小下一步：哪一个最小改动能产生新证据？

```

然后运行这个循环：

```text

预测改动后的行为

-&gt; 做最小改动

-&gt; 尽快运行

-&gt; 比较预期和实际

-&gt; 保留证据

-&gt; 决定下一个最小改动

```

一次只引入一个主要未知量。程序能运行后再扩展；一条端到端路径优先于许多没有连起来的局部代码。

## 从零开始一个项目

选择一个我自己或真实用户确实想用的小项目。它的第一版应能在几天而非几个月内产生一个可运行结果，同时允许日后自然长大。

如果我完全不会所选语言或工具：

1. 打开官方 Getting Started，只完成安装和最小运行示例。

2. 立即改变示例的输入、输出或行为，确认我知道是哪一处变化产生了结果。

3. 开始项目的第一条端到端路径；遇到具体缺口时再查对应知识。

第一版只回答：用户能否完成一次最核心的动作？暂时不追求完整架构、漂亮抽象或未来扩展性。

## 卡住协议：停止碰运气

满足任一条件时，我承认自己已经卡住：

- 连续两次修改都没有带来新证据；

- 我无法解释当前行为为什么发生；

- 我不知道应该观察哪里；

- 我正在重复搜索、复制和试错；

- 一个设计选择取决于我尚未确认的事实。

我先停止随机修改，保存当前错误和可复现状态，再写一张阻塞卡：

```text

目标：我想完成什么？

预期：我认为应该发生什么？为什么？

实际：实际发生了什么？完整错误是什么？

已知：哪些事实已由运行、测试或文档确认？

变化：问题出现前最后改变了什么？

未知：现在最需要回答的一个问题是什么？

```

把阻塞归入一个主要类型：

- **概念缺口**：术语或机制不明白。

- **接口缺口**：不知道某个语言、库或工具怎样使用。

- **行为缺口**：代码行为与我的预测不同。

- **环境缺口**：版本、依赖、配置、权限或运行环境有问题。

- **设计缺口**：存在多个可行方案，不知道约束和代价。

分类只用于选择下一种调查动作，不用于给自己贴能力标签。

## 查资料协议

我先把“我不会 X”改写成一个可以查证的问题，例如：

```text

宽泛：我不懂数据库。

可查：在当前版本的 SQLite 中，怎样保证同一次操作的两次写入要么都成功、要么都失败？

```

按以下顺序寻找答案：

1. 当前代码、测试、完整错误和调用链。

2. 项目的 README、注释、配置、依赖版本和本地帮助。

3. 官方文档、标准库文档和官方示例。

4. 协议规范、发布说明或相关成熟源码。

5. 针对具体问题的网络搜索、问题追踪器和技术讨论。

6. 一个能够确认或推翻假设的最小实验。

搜索词尽量包含：技术名称、实际版本、具体目标、关键行为或经过脱敏的错误原文。一次搜索只解决阻塞卡里的一个未知问题。

我不通读整份文档。先看目录和站内搜索，定位相关章节，然后只提取：

```text

它保证什么？

使用前提是什么？

最小示例是什么？

怎样失败？

是否与我的版本一致？

哪个实验能在当前项目中验证它？

```

博客和问答提供线索，官方文档、源码和实际运行负责确认。资料互相冲突时，我先检查版本和上下文，再设计能区分说法的实验。

## 最小实验协议

资料没有直接回答，或我不确定自己是否理解时，把问题从项目中缩成一个最小实验：

```text

假设：我认为原因是……

预测：如果假设成立，改变 X 后应该观察到……

控制：其余条件保持不变。

运行：只改变一个关键变量。

结果：记录实际输出，不改写记忆。

结论：假设被支持、被推翻，还是证据不足？

```

一个有价值的实验应能区分至少两个可能解释。得到结果后，把结论应用回真实项目并重新验证；实验成功不等于项目已经修好。

## 调试协议

调试的目标是找到“实际行为第一次偏离预期”的位置。

```text

稳定复现

-&gt; 缩小输入或路径

-&gt; 写下候选原因

-&gt; 添加日志、断言、测试或测量

-&gt; 用实验排除原因

-&gt; 修复根因

-&gt; 重跑原始复现

-&gt; 添加回归检查

```

每次修改前先写出预测。修改没有产生预期证据时，更新模型，不叠加更多猜测。错误消失后，我还要证明它是由预期修复消除的，而非环境变化或偶然条件掩盖。

## 学到够用的停止条件

当前知识满足下面五项时，我停止继续阅读并回到项目：

- 能用自己的话说明当前规则和原因；

- 能在项目里使用它，不依赖逐行复制；

- 项目行为已通过运行、测试或其他观测得到验证；

- 知道至少一个重要前提或失败边界；

- 知道需要更深入时该去哪里继续查。

我无需一次学完整个主题。暂时无关但感兴趣的内容记入停车场，只有项目真正需要或我主动选择深入时才打开。

## 每次解决问题后的最小沉淀

只记录以后可能复用的内容，控制在几行：

```text

问题：当时要解决什么？

规则：真正起作用的最小知识是什么？

证据：哪份文档、哪次实验或哪个测试支持它？

边界：它在什么条件下不成立？

下一步：回到项目要做的一个动作是什么？

```

我记录错误模型，而不收藏答案：我原来为什么会判断错？以后哪个信号能让我更早发现？

## 一次工作结束前

留下一个可以恢复的现场：

- 当前代码处于可运行状态，或明确记录为何不可运行；

- 已完成部分有运行结果、测试或用户行为作为证据；

- 变更保存在可回退的版本控制节点；

- 未解决问题保留完整错误、已试方案和当前假设；

- 下一次开始时只有一个具体动作，不写“继续研究”这类模糊事项。

## 项目里如何逐步接近 Senior

我不靠提前使用复杂技术证明水平。每当当前结果稳定后，从真实需要中选择一项新的责任：

```text

正确性：异常输入和失败路径会怎样？

可维护性：需求改变时，哪里最难改？

数据责任：数据如何保存、一致、迁移和恢复？

交付责任：别人能否重复构建、测试、部署和回滚？

运行责任：我怎样知道它正常、变慢或失败？

规模责任：真实负载出现了什么瓶颈？

安全责任：谁能做什么，敏感数据怎样保护？

产品责任：用户是否真的得到预期价值？

团队责任：别人能否理解、评审、使用和维护它？

```

只在真实约束出现时引入对应复杂度。为了获得高级能力证据，我需要让项目接触真实用户，持续维护，经历变化需求和故障，并逐渐与其他人协作。个人练习可以准备这些能力，不能替代真实责任。

## 向人求助时

独立调查达到卡住条件后，我可以向同事、社区或维护者求助。提问应包含：

```text

要实现的结果

最小复现或相关代码

环境与版本

预期行为和实际行为

完整且脱敏的错误

已经尝试的调查及结果

我目前的一个具体问题

```

获得回答后，我仍要查证、运行和解释。别人解决了问题不等于我掌握了它。

## 我的总循环

```text

选择一个真实、可观察的小结果

-&gt; 动手构建并尽快运行

-&gt; 卡住时写阻塞卡

-&gt; 查到解决当前问题所需的最小知识

-&gt; 用最小实验校准理解

-&gt; 应用回项目并验证

-&gt; 留下少量可迁移证据

-&gt; 交付给真实使用者

-&gt; 承担下一项自然出现的责任

```

当我不知道下一步做什么时，回到一个问题：

&gt; 现在最小的哪一个动作，能让我获得关于真实系统的新证据？

