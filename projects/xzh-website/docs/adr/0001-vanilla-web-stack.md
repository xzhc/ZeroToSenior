# 0001: Use Vanilla HTML, CSS, and JavaScript

We decided to build the personal website using pure semantic HTML5, modern vanilla CSS, and minimal vanilla JavaScript without frontend frameworks (React, Next.js, Astro) or build tooling.

This ensures zero build overhead, maximum longevity without dependency rot, instant page loads, and direct control over DOM accessibility and performance scores.
